// ====== slider js ==========

$('.slider-item').slick({
    // dots: true,
    arrows:false,
    infinite: true,
    speed: 300,
    slidesToShow: 3,
    // centerMode: true,
    // variableWidth: true,
    autoplay: true,
    autoplaySpeed: 3000,

  });